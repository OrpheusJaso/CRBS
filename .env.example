# Copy to .env to use PostgreSQL. If these are absent the app uses SQLite
# (crbs.db) automatically, so no .env is required to run the prototype.
SECRET_KEY=change-me-in-production

# DB_USER=crbs_user
# DB_PASS=crbs_pass
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=crbs
